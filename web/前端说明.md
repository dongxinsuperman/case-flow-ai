# 前端

Case Flow 的 Vue 3 前端。

## 本地启动

```bash
npm install
npm run dev
```

## UI 边界

- `stores` 保存业务状态。
- `graph` 把业务状态转换成脑图节点和边。
- `components` 渲染控件和面板。
- `pages` 组合路由级页面。

脑图必须保持为 case 数据的投影。业务更新应该通过类型化 API 调用和状态仓库完成。

## 场景化说明

比如用户点击“通过”，前端不应该只把卡片涂绿。它要调用 API 更新业务状态，状态仓库拿到新状态后，卡片和脑图再一起变绿。
