services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: case_flow
      POSTGRES_USER: case_flow
      POSTGRES_PASSWORD: case_flow
    ports:
      - "55432:5432"
    volumes:
      - case-flow-postgres:/var/lib/postgresql/data

volumes:
  case-flow-postgres:
